{
  "name": "@commit/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --experimental-strip-types --import ../../scripts/ts-esm.mjs src/server.ts",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run --config vitest.config.ts",
    "build": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@commit/domain": "workspace:*",
    "@electric-sql/pglite": "^0.3.11",
    "viem": "^2.37.6"
  },
  "devDependencies": {
    "@types/node": "^22.18.0",
    "typescript": "^5.9.2",
    "vitest": "^2.1.9"
  }
}
